## Custom HTML Generator from a set of words

This Node application reads some words from a file, and by using those words one by one, generates custom HTML content.

## Installation
``` bash
npm install
```

## Running Locally

``` bash
node --harmony htmlGenerator.js
```